// Задача 1. Функция принимает два аргумента var1 и var2.
//   1. Если оба аргумента - строки, то объединить их, вставить между ними запятую с пробелом ',' и вернуть строку
//   2. Если один или оба - не строки, вернуть строку 'invalid'
function combine (var1, var2) {
  //решение
}

// Задача 2. Фукнция принимает аргумент message в виде строки похожей на 'Hello.my.friend.!'
// заменить точки на пробелы. Вернуть строку.

function clean (message) {
//решение
}

module.exports = { combine, clean }